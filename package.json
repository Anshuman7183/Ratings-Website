{
  "scripts": {
    "dev": "concurrently \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "setup": "npm --prefix backend install && npm --prefix frontend install && powershell -Command \"if (-Not (Test-Path 'backend/.env')) { Copy-Item 'backend/.env.example' 'backend/.env' }\" && cd backend && npx prisma migrate dev --name init && npm run seed"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.12",
    "@types/node": "^24.3.0",
    "autoprefixer": "^10.4.21",
    "concurrently": "^9.2.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  }
}
